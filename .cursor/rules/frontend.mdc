---
description: Frontend development guide
globs: *.tsx
alwaysApply: true
---

## Acknowledgement
Frontend is at: <project_root>/web/, while backend is at <project_root>/server
Package management: pnpm
Icons: react-icons
Common UI: @/components/ui
All API definition: @/types/api

## How to use Toast
```
import { showSuccess, showError, showWarning, showInfo } from '@/utils/toast';

// usage
// showInfo: (message: string, duration?: number) => void;
```

## Misc
- use `bg-black/50` instead of `bg-opacity-50` in modal background
